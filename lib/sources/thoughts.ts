import type { ContentItem } from "../content-stream";

// Stub — will query the thoughts table via Drizzle once the DB schema is wired.
export async function getThoughts(): Promise<ContentItem[]> {
  return [];
}
